# Binaries — all builds go to bin/ via Makefile
bin/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binaries and coverage
*.test
coverage.out
coverage.html
*.coverprofile
coverage/

# Go workspace
go.work
go.work.sum

# Vendor (not used; keep tree clean if added later)
vendor/

# IDE and editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local env and credentials (never commit registry auth)
.env
.env.*
!.env.example
config.json
docker-config/

# Debug and profiling
*.log
debug
debug.test
*.pprof

# GoReleaser
/dist/

# RAG knowledge base data (populated by ocp-rag-ingest)
rag-data/
.ingest_meta.json

# Claude Code
CLAUDE.md
